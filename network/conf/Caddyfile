{

        email "{$ACME_EMAIL}"

        acme_dns porkbun {
                api_key "{$PORKBUN_API_KEY}"
                api_secret_key "{$PORKBUN_SECRET_API_KEY}"
        }

        log {
                output stdout
                level INFO
        }
}

(internal_service) {
        @internal {
                remote_ip 192.168.0.0/16 172.16.0.0/12 10.0.0.0/8
        }

        handle @internal {
                reverse_proxy {args[0]}
        }

        respond 404
      
}

# Global Authelia snippet
(authelia_auth) {
    forward_auth authelia:9091 {
        uri /api/authz/forward-auth
        copy_headers Remote-User Remote-Groups Remote-Name Remote-Email
    }
}

# Authelia Portal
auth.{$DOMAIN_NAME} {
    reverse_proxy authelia:9091
}

rss.{$DOMAIN_NAME} {
        reverse_proxy freshrss:80
}

adguard.{$DOMAIN_NAME} {
        reverse_proxy adguard:3000

}

mealie.{$DOMAIN_NAME} {
        reverse_proxy mealie:9000

}

wg-easy.{$DOMAIN_NAME} {
        # since the container will share the network with wg-easy
        # we can use the proper container name
        reverse_proxy wg-easy:80
}

internal.{$DOMAIN_NAME} {
        tls {
                dns porkbun {
                        api_key "{$PORKBUN_API_KEY}"
                        api_secret_key "{$PORKBUN_SECRET_API_KEY}"
                }
        }

        import internal_service homepage:3000
}

radarr.internal.{$DOMAIN_NAME} {
        tls {
                dns porkbun {
                        api_key "{$PORKBUN_API_KEY}"
                        api_secret_key "{$PORKBUN_SECRET_API_KEY}"
                }
        }

        import internal_service radarr:7878
        
}

sonarr.internal.{$DOMAIN_NAME} {
        tls {
                dns porkbun {
                        api_key "{$PORKBUN_API_KEY}"
                        api_secret_key "{$PORKBUN_SECRET_API_KEY}"
                }
        }

        import internal_service sonarr:8989
        
}


streaming.internal.{$DOMAIN_NAME} {
        tls {
                dns porkbun {
                        api_key "{$PORKBUN_API_KEY}"
                        api_secret_key "{$PORKBUN_SECRET_API_KEY}"
                }
        }

        import internal_service jellyfin:8096
        
}

overseerr.internal.{$DOMAIN_NAME} {
        tls {
                dns porkbun {
                        api_key "{$PORKBUN_API_KEY}"
                        api_secret_key "{$PORKBUN_SECRET_API_KEY}"
                }
        }

        import internal_service overseerr:5055
        
}

ersatztv.internal.{$DOMAIN_NAME} {
        tls {
                dns porkbun {
                        api_key "{$PORKBUN_API_KEY}"
                        api_secret_key "{$PORKBUN_SECRET_API_KEY}"
                }
        }

        import internal_service ersatztv:8409
        
}

speedtest.internal.{$DOMAIN_NAME} {
        tls {
                dns porkbun {
                        api_key "{$PORKBUN_API_KEY}"
                        api_secret_key "{$PORKBUN_SECRET_API_KEY}"
                }
        }

        import internal_service speedtest:80
}


wiki.{$DOMAIN_NAME} {
        import authelia_auth
        reverse_proxy docmost:3000
}

photos.{$DOMAIN_NAME} {
        reverse_proxy immich_server:2283
}

cloud.{$DOMAIN_NAME} {
        reverse_proxy nextcloud-aio-apache:11000
}

fitness.{$DOMAIN_NAME} {
        reverse_proxy sparkyfitness-frontend:80
}

mlflow.internal.{$DOMAIN_NAME} {
        import authelia_auth

        tls {
                dns porkbun {
                        api_key "{$PORKBUN_API_KEY}"
                        api_secret_key "{$PORKBUN_SECRET_API_KEY}"
                }
        }

        import internal_service mlflow:5000
}